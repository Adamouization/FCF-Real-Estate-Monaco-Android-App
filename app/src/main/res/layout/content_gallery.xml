<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorBlackBackground"
    android:fitsSystemWindows="true">

    <android.support.v4.view.ViewPager
        android:id="@+id/pager"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginBottom="@dimen/gallery_viewPager_marginBottom"
        android:fitsSystemWindows="true">

    </android.support.v4.view.ViewPager>

    <ImageButton
        android:id="@+id/btn_close"
        style="@style/Widget.AppCompat.ActionButton"
        android:layout_width="?attr/actionBarSize"
        android:layout_height="?attr/actionBarSize"
        android:layout_gravity="top|right"
        android:layout_marginTop="@dimen/gallery_imageButton_marginTop"
        android:scaleType="fitCenter"
        android:src="@drawable/ic_keyboard_backspace" />

    <HorizontalScrollView
        android:layout_width="match_parent"
        android:layout_height="@dimen/gallery_scrollview_height"
        android:layout_gravity="bottom"
        android:background="@color/colorBlackBackground">

        <LinearLayout
            android:id="@+id/thumbnails"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:gravity="center_vertical"
            android:orientation="horizontal"
            android:paddingTop="@dimen/gallery_scrollview_linla_padding" />
    </HorizontalScrollView>
</FrameLayout>